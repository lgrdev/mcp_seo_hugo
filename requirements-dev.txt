# Dépendances de test uniquement. Le plugin lui-même n'en a aucune : scripts/seoctl.py
# et scripts/seolib/ n'utilisent que la bibliothèque standard.
# PyYAML sert d'oracle dans tests/test_frontmatter_oracle.py, qui compare le mini-lecteur
# de front matter à un vrai parseur YAML sur le corpus réel.
pytest>=8,<9
PyYAML>=6,<7
