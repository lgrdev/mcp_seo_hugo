# Dépendances pour lancer les tests. fastmcp et chromadb sont remplacés par des
# stubs dans tests/conftest.py : les tests portent sur la logique pure, pas sur
# le serveur MCP ni sur l'index vectoriel, et n'ont donc pas besoin de torch.
pytest>=8,<9
python-frontmatter>=1.3.0,<2
networkx>=3.6.1,<4
